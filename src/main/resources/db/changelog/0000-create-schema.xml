<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">
    <!--
    Схема public используется по умолчанию в PostgreSQL.
    Все таблицы создаются в схеме public.
    -->
    <changeSet id="0000-create-schema" author="Filimontsev_VV">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="users" schemaName="public"/>
            </not>
        </preConditions>
        <!-- Схема public существует по умолчанию в PostgreSQL, создание не требуется -->
        <comment>Using default public schema</comment>
    </changeSet>
</databaseChangeLog>